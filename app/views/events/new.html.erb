<section class="main">
  <div class="page-info hard-shadows">
        <span>ライブ登録</span>
  </div>
  <div class="container">
    <%= form_for(@event, method: :post) do |f| %>
      <%= render :partial => "event_edit", locals: { event: @event, f: f, event_performers: "" } %>
    <% end %>
  </div>
</section>
